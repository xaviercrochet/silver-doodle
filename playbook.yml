- hosts: localhost
  tasks:
  - name: Deploy the service
    k3s:
      state: present
      definition: ""
      namespace: default
  - name: Deploy the application
    k3s:
      state: present
      namespace: default
      definition: ""